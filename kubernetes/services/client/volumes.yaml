apiVersion: apps/v1
kind: Deployment
metadata:
  name: <template>
spec:
  template:
    spec:
      containers:
        - name: <template>
          volumeMounts:
            - name: client-config
              mountPath: /opt/app/config.json
      volumes:
        - name: client-config
          configMap:
            name: client-config
          